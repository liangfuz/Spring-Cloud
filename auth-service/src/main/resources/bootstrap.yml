server:
  port: 9060
spring:
  application:
    name: auth-service
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8888/
      username: user
      password: 123456
      fail-fast: true
  session:
    store-type: redis
