spring:
  application:
    name: gateway
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:8888/
      username: user
      password: 123456
      fail-fast: true

#rabbitMq
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest